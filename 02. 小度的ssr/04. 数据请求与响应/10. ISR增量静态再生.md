

### 概述
<font style="color:rgb(28, 30, 33);">无需重建整个站点即可更新静态内容，通过为大多数请求提供预渲染的静态页面来减少服务器负载。</font>

<font style="color:rgb(28, 30, 33);"></font>

### <font style="color:rgb(28, 30, 33);">基于时间的重新验证</font>
+ fetch `{ next: { revalidate: 10 } }`
+ 页面  `export const revalidate = 10`



### 基于按需的重新验证
+ revalidatePath
+ revalidateTag



