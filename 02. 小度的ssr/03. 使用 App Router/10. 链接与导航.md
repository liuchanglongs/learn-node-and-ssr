### useRouter导航

- Link（推荐），客户端和服务端都可以使用
- useRouter，仅客户端可以使用，特殊需求可以采用

### useSearchParams与usePathname

```js
const searchParams = useSearchParams();
const pathname = usePathname();
console.log(searchParams.toString());
onsole.log(searchParams.get("type"));

const params = new URLSearchParams(searchParams.toString());
params.set("type", "drink");

// 更新query参数:在当前位置后面再压一条新记录 → 用户点「后退」会回到上一页。
window.history.pushState(null, "", `${pathname}?${params.toString()}`);
// 替代更新query参数，浏览器回退无用: 只改当前这一条的内容（URL / state）→ 栈的长度不变，上一页、上上页都还在。
window.history.replaceState(null, "", `${pathname}?${params.toString()}`);
```

### 预取数据

- 默认情况动态组件只会预取loading
  prefetch -> 在生产环境下会预取数据

router.prefetch() -> 也可以预取数据
![alt text](https://raw.githubusercontent.com/lcllpz/storage-imgs/main/nuxtr/1735806492616-c54ac072-0085-4807-b1aa-8605d43e61178.png)
