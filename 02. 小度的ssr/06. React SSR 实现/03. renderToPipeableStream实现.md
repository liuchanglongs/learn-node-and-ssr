

### 概述
renderToPipeableSteam将一个React组件树渲染为管道化（pipeable）的Node.js流。



renderToString的问题，无法支持suspense操作。

### 开发步骤
1. entry-server.jsx改造
+ 使用renderToPipeableStream方法返回pipe对象。



2. server.js改造
+ onShellReady回调，在shell初始化渲染后立即调用。
+ onShellError和onError回调。





